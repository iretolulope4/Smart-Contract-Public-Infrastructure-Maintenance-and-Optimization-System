[project]
name = 'Smart-Contract-Public-Infrastructure-Maintenance-and-Optimization-System'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.bridge-safety-monitor]
path = 'contracts/bridge-safety-monitor.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
